import { Router } from 'express'; import { listNotes, createNote, updateNote, updatePosition, deleteNote } from '../controllers/note.controller.js'; import { authenticate } from '../middleware/auth.middleware.js'; import { validateNote } from '../middleware/validate.middleware.js'; const router = Router(); router.use(authenticate); router.route('/').get(listNotes).post(validateNote, createNote); router.route('/:id').put(validateNote, updateNote).delete(deleteNote); router.patch('/:id/position', updatePosition); export default router;
