# Tablero_Notas
Portal de equipo con tablero de notas. Aplicación web con gestión de usuarios y roles, tablero compartido tipo post-it con estados y posiciones persistentes, dashboard con métricas vía AWS Lambda y arquitectura desplegable en local con Docker Compose y en AWS con SAM/CloudFormation.
